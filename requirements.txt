# ================================================================================
# River Kova — requirements.txt
# Python 3.11+ dependencies
# Install: pip install -r requirements.txt
# ================================================================================

# ── ROS2 / MoveIt (installed via apt/rosdep, listed for reference) ────────────
# rclpy                   # ROS2 Python client — installed via ROS2 Humble
# moveit                  # MoveIt2 Python bindings — installed via ROS2

# ── Computer Vision ───────────────────────────────────────────────────────────
opencv-python==4.9.0.80
mediapipe==0.10.14
numpy==1.26.4
pyrealsense2==2.55.1.6486       # Intel RealSense SDK Python bindings

# ── Hardware / Serial ─────────────────────────────────────────────────────────
pyserial==3.5

# ── Web / API ─────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
requests==2.32.3
urllib3==2.2.1
# Note: the LLM initiative brain runs on the River Song server, reached over
# the existing HTTP client — the robot needs no model SDK or API key.

# ── Data / Config ─────────────────────────────────────────────────────────────
pydantic==2.7.1
pyyaml==6.0.1

# ── Telemetry ─────────────────────────────────────────────────────────────────
influxdb-client==1.44.0
psutil==5.9.8

# ── Networking / VPN ─────────────────────────────────────────────────────────
# WireGuard is managed via wg-quick (system package, not pip)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.1
pytest-mock==3.14.0
pytest-timeout==2.3.1
