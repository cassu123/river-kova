# ================================================================================
# River Kova — requirements-sim.txt
# Minimal dependencies for SIMULATION mode on any laptop/desktop (no robot,
# no ROS2, no camera SDKs). Install: pip install -r requirements-sim.txt
# For a real unit, use the full requirements.txt instead.
# ================================================================================

numpy==1.26.4

# ── Web / API ─────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
requests==2.32.3
pydantic==2.7.1

# ── Testing ───────────────────────────────────────────────────────────────────
httpx==0.27.0                   # FastAPI TestClient transport
pytest==8.2.1
pytest-mock==3.14.0
pytest-timeout==2.3.1
